function addMulfunc(a, b, c=7){

    //to add
    console.log(`The addition of two number is : ${a + b + c}`);

    //to multiply
    console.log(`The multiplication of two number is : ${a * b * c}`);
}

addMulfunc(30, 5);